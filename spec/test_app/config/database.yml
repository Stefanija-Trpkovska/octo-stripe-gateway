default: &default
  adapter: postgresql
  host: 127.0.0.1
  username: postgres
  password: <%= ENV.fetch("POSTGRES_PASSWORD", "password") %>

development:
  <<: *default
  database: osg_development

test:
  <<: *default
  database: osg_test

production:
  <<: *default
  database: osg_production
